# SportsCenter
ng build -c production
git add .
git commit -m ""
git push origin main --force

# Definición
TABLA LEVEL:
he pensado que el nivel debe ser por deporte. Así, tendre una tabla que sea id_level, sport_id, level_name. 
